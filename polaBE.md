# Conversation & Chat

// Get All Conversation (GET /)

// Get One Conversation with Chat (GET /conversation/:id)

// Create Conversation (POST /conversation)

// Delete One Conversation with Chat (DELETE /conversation:id)

# Token 

// Get Token (GET /token)

// Add Token (POST /token:id)

// Update Token (PUT /token:id)

// Delete Token (DELETE /token:id)

# Auto Deploy

// Deploy (POST /deploy)